server:
  port: 8083

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/RatingService
    username: postgres
    password: 911850
  application:
    name: RATINGSERVICE
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  config:
    import: optional:configserver:http://localhost:8085
okta:
  oauth2:
    issuer: https://integrator-4443484.okta.com/oauth2/default
    audience: api://default
